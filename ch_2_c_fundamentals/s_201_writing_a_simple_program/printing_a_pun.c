#include <stdio.h> // commands that begin with # are known as directives and are passed to the preprocessor when the file it's located in is executed.

int main() {
    printf("To C, or not to C? that is the question!\n");
    return 0;
}
